# Dependencies
node_modules/

# Environment / secrets — NEVER commit these
.env
.env.local
.env.*.local

# Local SQLite database (root-level only)
/data/

# TLS certificates — generated locally with mkcert
certs/

# Server runtime log
server.log

# Build artefacts that are re-generated (but docs/ itself IS committed for Pages)
scripts/db-stub.js

# OS
.DS_Store
Thumbs.db

# Editors
.vscode/
.idea/
*.swp
