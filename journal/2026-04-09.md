# Project Journal — 2026-04-09

## Setup & Configuration

- Cloned repository from `https://github.com/s2kdesign-com/seasky-penthouse.git`
- Installed 170 npm packages (0 vulnerabilities)
- Generated self-signed TLS certificate (RSA 2048-bit, valid 1 year) for `localhost`
- Created `.env` configuration with session secret and generated VAPID keys
- Configured Google OAuth credentials (Client ID, Client Secret, API Key)
- Updated config database to reflect real Google OAuth Client ID

## Build

- Compiled WebAssembly module: `wasm/calendar.wat` → `src/public/calendar.wasm` (289 bytes)
- Ran static export: generated `docs/` with 13 events and 4 JSON data files (events.json, status.json, feeds.json, config.json)

## Server Startup

- Started Express server on HTTPS :3443 with HTTP :3000 redirect
- Initial calendar sync pulled 13 events:
  - Airbnb: 2 events
  - Booking.com: 8 events
  - Official Website: 3 events
- Cron job registered for hourly feed sync

## Validation — Public API Endpoints

| Endpoint | Method | Result |
|---|---|---|
| `/api/events` | GET | 13 events returned with proper structure |
| `/api/status` | GET | Sync status with 3 feeds, all "ok" |
| `/api/feeds` | GET | 3 feeds with id, name, color |
| `/api/config` | GET | Public config (BASE_URL, GOOGLE_CLIENT_ID, VAPID key) |
| `/api/me` | GET | Returns `null` when unauthenticated |
| `/api/push/vapid-public-key` | GET | VAPID public key returned |

## Validation — Admin Endpoints (Auth Guard)

All 5 admin endpoints correctly return **403 Forbidden** without authentication:

- `POST /api/sync`
- `PATCH /api/events/:id/override`
- `GET /api/admin/users`
- `GET /api/admin/logs`
- `GET /api/admin/config`

## Validation — Push Notification Endpoints

| Endpoint | Method | Result |
|---|---|---|
| `/api/push/subscribe` | POST (valid payload) | Subscription saved |
| `/api/push/subscribe` | POST (invalid payload) | Rejected with error |
| `/api/push/subscribe` | DELETE | Unsubscribe successful |

## Validation — Frontend

- `GET /` (index.html) — 200 OK, FullCalendar dashboard loads
- `GET /login.html` — 200 OK, Google OAuth login page loads
- HTTP → HTTPS redirect — 301 from :3000 to :3443

## Validation — Google OAuth

- `GET /auth/google` redirects to `accounts.google.com` with correct client ID
- Callback URL set to `https://localhost:3443/auth/google/callback`

## Validation — Database

SQLite database auto-created at `data/seasky.db` with correct schema:

- `users` — id, google_id, name, email, avatar, role, created_at, last_login
- `event_overrides` — event_id, start_dt, end_dt, updated_by, updated_at
- `push_subscriptions` — id, endpoint, p256dh, auth, created_at
- `activity_logs` — id, user_id, user_name, action, details, created_at
- `config` — key, value, is_public, updated_at

## Validation — WebAssembly

All 3 exported WASM functions tested and working:

- `nightCount(Apr1, Apr4)` = 3 nights
- `daysOverlap(Apr1-5, Apr3-7)` = 1 (overlap detected)
- `isToday(UTC midnight today, now)` = 1 (correct)

## Validation — TLS Certificate

- Protocol: TLSv1.3
- Cipher: TLS_AES_256_GCM_SHA384
- Key: RSA 2048-bit, SHA-256 signature
- Status: Self-signed, valid Apr 9 2026 — Apr 9 2027
