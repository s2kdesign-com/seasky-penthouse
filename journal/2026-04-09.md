# Project Journal — 2026-04-09

## Setup & Configuration

- Cloned repository from `https://github.com/s2kdesign-com/seasky-penthouse.git`
- Installed 170 npm packages (0 vulnerabilities)
- Generated self-signed TLS certificate (RSA 2048-bit, valid 1 year) for `localhost`
- Created `.env` configuration with session secret and generated VAPID keys
- Configured Google OAuth credentials (Client ID, Client Secret, API Key)
- Updated config database to reflect real Google OAuth Client ID

## Build

- Compiled WebAssembly module: `wasm/calendar.wat` → `src/public/calendar.wasm` (289 bytes)
- Ran static export: generated `docs/` with 13 events and 4 JSON data files (events.json, status.json, feeds.json, config.json)

## Server Startup

- Started Express server on HTTPS :3443 with HTTP :3000 redirect
- Initial calendar sync pulled 13 events:
  - Airbnb: 2 events
  - Booking.com: 8 events
  - Official Website: 3 events
- Cron job registered for hourly feed sync

## Validation — Public API Endpoints

| Endpoint | Method | Result |
|---|---|---|
| `/api/events` | GET | 13 events returned with proper structure |
| `/api/status` | GET | Sync status with 3 feeds, all "ok" |
| `/api/feeds` | GET | 3 feeds with id, name, color |
| `/api/config` | GET | Public config (BASE_URL, GOOGLE_CLIENT_ID, VAPID key) |
| `/api/me` | GET | Returns `null` when unauthenticated |
| `/api/push/vapid-public-key` | GET | VAPID public key returned |

## Validation — Admin Endpoints (Auth Guard)

All 5 admin endpoints correctly return **403 Forbidden** without authentication:

- `POST /api/sync`
- `PATCH /api/events/:id/override`
- `GET /api/admin/users`
- `GET /api/admin/logs`
- `GET /api/admin/config`

## Validation — Push Notification Endpoints

| Endpoint | Method | Result |
|---|---|---|
| `/api/push/subscribe` | POST (valid payload) | Subscription saved |
| `/api/push/subscribe` | POST (invalid payload) | Rejected with error |
| `/api/push/subscribe` | DELETE | Unsubscribe successful |

## Validation — Frontend

- `GET /` (index.html) — 200 OK, FullCalendar dashboard loads
- `GET /login.html` — 200 OK, Google OAuth login page loads
- HTTP → HTTPS redirect — 301 from :3000 to :3443

## Validation — Google OAuth

- `GET /auth/google` redirects to `accounts.google.com` with correct client ID
- Callback URL set to `https://localhost:3443/auth/google/callback`
- User "Svetlin Krastanov" successfully logged in via Google OAuth on live deployment
- Session cookie (HMAC-SHA256) validated, user stored in D1 with role `admin`

## Validation — Database

SQLite database auto-created at `data/seasky.db` with correct schema:

- `users` — id, google_id, name, email, avatar, role, created_at, last_login
- `event_overrides` — event_id, start_dt, end_dt, updated_by, updated_at
- `push_subscriptions` — id, endpoint, p256dh, auth, created_at
- `activity_logs` — id, user_id, user_name, action, details, created_at
- `config` — key, value, is_public, updated_at

## Validation — WebAssembly

All 3 exported WASM functions tested and working:

- `nightCount(Apr1, Apr4)` = 3 nights
- `daysOverlap(Apr1-5, Apr3-7)` = 1 (overlap detected)
- `isToday(UTC midnight today, now)` = 1 (correct)

## Validation — TLS Certificate

- Protocol: TLSv1.3
- Cipher: TLS_AES_256_GCM_SHA384
- Key: RSA 2048-bit, SHA-256 signature
- Status: Self-signed, valid Apr 9 2026 — Apr 9 2027

## Cloudflare Workers Deployment

- Ported entire Express app to Cloudflare Workers + D1 database
- Custom ICS parser (`ics.js`) replacing node-ical for Workers compatibility
- Manual Google OAuth flow (`auth.js`) replacing Passport.js
- HMAC-SHA256 signed cookie sessions replacing express-session
- D1 database with 7 tables: users, push_subscriptions, activity_logs, event_overrides, config, events_cache, sync_status
- Hourly cron trigger for feed sync + auto-sync on first request
- Live at: `https://seasky-penthouse.coingardenworld.workers.dev`

## SEO Meta Tags

- Added comprehensive SEO to `index.html` and `login.html`:
  - Open Graph tags (og:title, og:description, og:image, og:site_name)
  - Twitter Card tags (summary_large_image)
  - Canonical URL → `https://seasky-apartments.com/`
  - OG image from seasky-apartments.com hero photo
  - `robots: noindex, nofollow` (admin dashboard)
  - `theme-color: #000000`, apple-touch-icon
- Updated header logo links to point to `https://seasky-apartments.com/`

## Calendar Redesign — Property Booking Style

Transformed FullCalendar into a vacation-rental availability calendar:

- **Day cell coloring**: past (diagonal hatching), today (gold circle + border), booked (warm yellow tint), available (clean)
- **Check-in/check-out**: diagonal split backgrounds on transition days
- **Booking pills**: source color dot + name + night count (e.g. "● Booking.com 4n")
- **Occupancy stats**: total bookings, upcoming, occupancy % with progress bar
- **Property legend**: Past | Today | Booked | Available | Check-in | Check-out + source dots
- **Views**: month + year (removed week/list views)

## Repository Consolidation

Merged `seasky-calendar` (Workers deployment) back into `seasky-penthouse` (single source of truth):

- `src/` — Express server (local dev, `npm start`)
- `worker/src/` — Cloudflare Workers (production, `wrangler.jsonc`)
- `src/public/` — Shared static assets (used by both)
- Replaced `wrangler.toml` → `wrangler.jsonc`
- Updated `name` to `seasky-penthouse`, `BASE_URL` to new workers.dev URL
- Updated D1 config table BASE_URL
- Deleted old `seasky-calendar` worker from Cloudflare
- Updated Google OAuth redirect URI for new URL
- Cloudflare Git integration reconnected to `s2kdesign-com/seasky-penthouse`

## Account Page

Added new Account page for signed-in users:

- **Profile card**: large avatar with gold border, name, email, role badge
- **Session card**: sign-in provider (Google icon), member since date, last login timestamp
- **Danger zone**: red-bordered card with sign-out button
- **Not signed in state**: placeholder avatar + "Sign in with Google" prompt
- **Navigation**: "Account" nav item (person icon) visible only when authenticated
- **i18n**: full EN/BG translations
- **Responsive**: mobile layout with stacked profile card

## Links Page & Social Footer

Added Links page and global footer:

- **Links page** (`nav.links` — external link icon in sidebar):
  - "Like us & Leave a comment" section: Facebook, Instagram, Google review
  - "Rent the apartment" section: Official Website, Booking.com, Airbnb
  - Branded icon cards with hover animation (slide right + accent border)
- **Footer** (inside `<main>`, `margin-top: auto`):
  - Social icons: Facebook, Instagram, Google, SeaSky logo, Booking.com, Airbnb
  - Copyright: "2026 SeaSky Apartments - Burgas, Bulgaria"
  - Dark background matching header, responsive stacking on mobile
- **i18n**: full EN/BG translations for all link labels and descriptions
