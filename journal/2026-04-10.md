# Project Journal — 2026-04-10

## Bug Fix: Calendar Events Off-by-One Day

**Problem:** All calendar events were displaying one day ahead. For example, a Booking.com reservation ending April 14 was shown ending April 15 on the dashboard.

**Root Cause:** The `localDateWithTime()` function in both `worker/src/ics.js` and `src/calendarService.js` used JavaScript's local time methods (`.getFullYear()`, `.getMonth()`, `.getDate()`) on UTC date objects. When the server/worker timezone is ahead of UTC (e.g., Europe/Sofia = UTC+3), midnight UTC dates like `2026-04-14T21:00:00.000Z` were interpreted as April 15 in local time instead of April 14 in UTC.

**Fix:** Changed to UTC date methods (`.getUTCFullYear()`, `.getUTCMonth()`, `.getUTCDate()`) in both files:
- `worker/src/ics.js` (Cloudflare Workers production)
- `src/calendarService.js` (Express local dev)

**Files Changed:**
- `worker/src/ics.js` — line 148-152: `localDateWithTime()` uses UTC methods
- `src/calendarService.js` — line 50-56: `localDateWithTime()` uses UTC methods

**Validation:**
- Local server restarted and `/api/events` confirmed: `rawEnd: "2026-04-14T21:00:00.000Z"` → `end: "2026-04-14T11:00"` (was previously `2026-04-15T11:00`)
- Property correctly shows free after April 14 at 11:00am, matching Airbnb source calendar

## Feature: ICS Feeds Configuration Page

**What:** Admin-only page to dynamically configure ICS calendar feeds (add, edit, remove feeds and trigger sync) without code changes.

**Architecture:**
- Feeds stored as JSON array in D1 config key `ICS_FEEDS` (private)
- `worker/src/ics.js` exports `getFeeds(d1)` — reads from D1, falls back to hardcoded defaults
- `worker/src/ics.js` exports `saveFeeds(d1, feeds)` — persists feeds array to D1
- `syncAllFeeds()` now reads feeds dynamically via `getFeeds(d1)` instead of using hardcoded `FEEDS` const

**API Endpoints:**
- `GET /api/admin/feeds` — returns full feeds array (admin only)
- `PUT /api/admin/feeds` — saves feeds array `{ feeds: [...] }` (admin only)

**Frontend:**
- Nav item: `data-page="feed-config"` with RSS icon, under admin-only section
- Page: `#page-feed-config` with dynamic feed rows (id, name, URL, color picker, remove button)
- `loadFeedConfigPage()` — loads current feeds, renders editable rows, handles save + sync
- Add Feed button creates empty row; Save persists to D1; Sync Now triggers `/api/sync`
- i18n: 18 new keys in both EN and BG (`feedConfig.*`, `nav.feedConfig`)

**CSS:**
- `.feed-config-form`, `.feed-config-row`, `.feed-row-fields` — card-based layout
- `.feed-color-input` — native color picker styled to match theme
- `.btn-feed-remove` — red trash icon button with hover state
- `.btn-secondary` — outline style for Sync Now button
- Mobile responsive: feed fields stack vertically at 768px

**Files Changed:**
- `worker/src/ics.js` — dynamic feeds via `getFeeds()`/`saveFeeds()`, `syncAllFeeds()` reads from D1
- `worker/src/index.js` — added `GET/PUT /api/admin/feeds` routes
- `src/calendarService.js` — `FEEDS` now mutable via `setFeeds()`, exported
- `src/server.js` — added `GET/PUT /api/admin/feeds` Express routes
- `src/public/index.html` — nav item + page HTML
- `src/public/app.js` — i18n keys (EN/BG) + `loadFeedConfigPage()` + navigateTo dispatch
- `src/public/style.css` — feed config styles + mobile responsive

**Validation:**
- `npm run build` succeeds (12 events synced)
- `/api/feeds` returns 3 feeds correctly
- Events still show correct dates (off-by-one fix preserved)
- EN and BG translations added for all new UI text

## Performance: Timezone Selector Moved Server-Side

**Problem:** Settings page loaded slowly because `initTimezone()` computed UTC offsets for 418 IANA timezones client-side on every page visit, calling `Intl.DateTimeFormat` for each one.

**Fix:** Pre-compute the timezone list server-side and serve via a new API endpoint. Frontend fetches the cached result instead of computing it.

**API Endpoint:** `GET /api/timezones`
- Returns JSON array: `[{ tz, label, off }, ...]` (418 entries, sorted by UTC offset)
- Server caches result in memory (computed once)
- `Cache-Control: public, max-age=86400` (24h browser cache)

**Files Changed:**
- `src/server.js` — added `GET /api/timezones` with in-memory cache (`_tzCache`)
- `worker/src/index.js` — added matching `GET /api/timezones` for Cloudflare Workers
- `src/public/app.js` — `initTimezone()` now async, fetches from `/api/timezones` (or `data/timezones.json` in static mode). Removed `COMMON_TIMEZONES` constant and client-side `Intl.supportedValuesOf` computation
- `scripts/export-static.js` — generates `docs/data/timezones.json` during static build

**Validation:**
- `/api/timezones` returns 418 timezones with correct offsets (e.g. `Europe/Sofia (GMT+3)`)
- `npm run build` succeeds, `docs/data/timezones.json` generated (31KB)
- Settings page timezone dropdown renders correctly from server data

## Fix: Header Logo Points to Dashboard

**Problem:** The header logo link pointed to `https://seasky-apartments.com/` (external site), opening in a new tab.

**Fix:** Changed to navigate to the dashboard page within the SPA instead.

**Files Changed:**
- `src/public/index.html` — logo `<a>` now uses `onclick="navigateTo('dashboard')"` instead of external URL
- `docs/index.html` — rebuilt

## Feature: Stats Cards Visible Only for Logged-In Users

**Problem:** The stats cards (Total Bookings, Upcoming, Occupancy, feed sync status) were visible to all visitors, including unauthenticated users.

**Fix:** Added `auth-only` CSS class to `stats-row`. Cards are hidden by default and shown only when a user is signed in, using the same visibility pattern as `nav-auth-only`.

**Files Changed:**
- `src/public/index.html` — added `auth-only` class to `#stats-row`
- `src/public/style.css` — added `.auth-only` / `.auth-only.visible` rules
- `src/public/app.js` — `init()` adds `visible` class to `.auth-only` elements when `currentUser` exists

**Validation:**
- `npm run build` succeeds
- Stats row hidden when not logged in, visible after sign-in

## Fix: Stats Cards Grid Layout on Laptop

**Problem:** Stats cards were stacking vertically (one per row) on laptop screens because `.auth-only.visible` was forcing `display: grid !important` without proper grid columns, overriding the original flex layout.

**Fix:** 
- Changed `.stats-row` from `display: flex` to `display: grid` with `grid-template-columns: repeat(auto-fill, minmax(180px, 1fr))` — responsive grid that fills available width
- Fixed `.auth-only.visible` to use `display: initial !important` generically, with a specific `.stats-row.auth-only.visible` rule for `display: grid !important`
- Reduced card padding from `16px 20px` to `14px 16px` for compactness

**Files Changed:**
- `src/public/style.css` — grid layout for stats-row, auth-only visibility fix

**Validation:**
- 6 cards display in a single row on laptop (1315px viewport)
- Mobile 2-column grid preserved at 768px breakpoint

## Feature: Booking Inquiry Backend (API + Database)

**What:** Added booking inquiry submission and management -- database schema, DB functions, and API endpoints for both Cloudflare Workers and Express local dev. Frontend not yet implemented.

**Database:**
- New `booking_inquiries` table: id, check_in, check_out, guests, name, email, phone, comment, user_id, status (pending/confirmed/declined), created_at
- Added `phone` column to `users` table (ALTER TABLE with try-catch for idempotency)

**DB Functions (worker/src/db.js + src/db.js):**
- `addInquiry(checkIn, checkOut, guests, name, email, phone, comment, userId)` -- insert new inquiry
- `getInquiries()` -- all inquiries ordered by created_at DESC, limit 500
- `updateInquiryStatus(id, status)` -- update status field
- `updateUserPhone(id, phone)` -- update user phone number

**API Endpoints:**
- `POST /api/inquiries` (public) -- submit inquiry, validates required fields, attaches user_id if logged in
- `GET /api/admin/inquiries` (admin) -- list all inquiries
- `PATCH /api/admin/inquiries/:id/status` (admin) -- update inquiry status, logs action
- `PATCH /api/me/phone` (auth required) -- update current user's phone number
- `GET /api/me` -- now also returns `created_at`, `last_login`, and `phone` fields

**Files Changed:**
- `worker/src/db.js` -- booking_inquiries table, phone column migration, addInquiry/getInquiries/updateInquiryStatus/updateUserPhone
- `worker/src/index.js` -- 4 new routes + /api/me fix
- `src/db.js` -- matching local SQLite schema + functions
- `src/server.js` -- matching Express routes + /api/me fix

**Validation:**
- `npm run build` succeeds (13 events synced)

## Feature: Booking Inquiry Frontend (Calendar Selection + Modal + Account Phone)

**What:** Added calendar date selection, booking inquiry modal, and phone number management on the account page. Users can select dates on the calendar to open a booking inquiry form.

**Calendar Selection (app.js):**
- FullCalendar options: `selectable: true`, `selectMirror: true`, `selectAllow` (blocks past dates)
- `select` callback opens `openInquiryModal(startStr, endStr)` then unselects

**Inquiry Modal (app.js):**
- `openInquiryModal()` renders into existing modal-body: check-in/check-out dates, guest selector, name/email (hidden if logged in), phone, comment, submit button
- Submits to `POST /api/inquiries` with JSON body
- Shows success/error status inline
- If not logged in, shows Google sign-in divider below form

**Account Phone Section (app.js + index.html):**
- New `#account-phone-card` between session and danger zone in index.html
- `loadAccountPage()` renders phone input + save button, calls `PATCH /api/me/phone`
- Hidden when not logged in (both early return and else branch)

## Enhancement: Editable Dates in Booking Modal

**Problem:** Dates in the booking inquiry modal were static text — users couldn't adjust them after selecting on the calendar.

**Fix:** Replaced static date display with `<input type="date">` pickers. Check-in/check-out dates are now editable inside the modal. Changing dates auto-updates the nights count and hidden form fields. Check-out min is enforced to be after check-in.

**Files Changed:**
- `src/public/app.js` — `openInquiryModal()` now renders date inputs instead of text, added `updateInquiryDates()` listener
- `src/public/style.css` — added `.inquiry-date-input` styles with dark theme calendar picker

**Validation:**
- Single click on calendar opens modal with editable dates
- Drag across multiple days opens modal with correct range
- Changing check-in auto-adjusts check-out min and nights count

**i18n:** 18 new keys in both EN and BG (`inquiry.*`, `account.phone*`)

**CSS (style.css):**
- `.inquiry-modal`, `.inquiry-dates`, `.inquiry-date-box`, `.inquiry-field`, `.btn-inquiry-submit`, `.inquiry-status`, `.inquiry-divider`, `.inquiry-google-btn`
- `.account-phone-input-row` for phone save row
- Mobile responsive: `.inquiry-dates` wraps, `.inquiry-date-box` flex with min-width

**Files Changed:**
- `src/public/app.js` -- i18n keys, selectable calendar, openInquiryModal(), account phone section
- `src/public/index.html` -- phone card HTML in account page
- `src/public/style.css` -- inquiry modal styles + account phone row + mobile responsive
- `docs/` -- rebuilt static export

**Validation:**
- `npm run build` succeeds (13 events synced)
- All i18n keys added for both EN and BG

## Enhancement: Reservations Page — Combined ICS Feeds + Inquiries

**Problem:** The Reservations page only showed booking inquiries submitted through the website. Admins had no unified view of all reservations from Airbnb, Booking.com, and the official website alongside manual inquiries.

**Fix:** The Reservations page now fetches both `/api/admin/inquiries` and `/api/events` in parallel, combines them into a unified list sorted by date descending, with filter tabs to switch between All, Inquiries only, or Feeds only.

**Features:**
- Filter tabs: All (combined count), Inquiries (count), Feeds (count) — pill-button style
- ICS feed events show colored source badges (Airbnb, Booking.com, etc.) matching their calendar color, with event title and description
- Booking inquiries show yellow "Inquiries" badge with status dropdown (Pending/Confirmed/Declined)
- Cards use left border color to visually distinguish sources
- Sorted by start/check-in date descending (newest first)

**i18n:** 7 new keys in both EN and BG (`reservations.sectionInquiries`, `reservations.sectionIcal`, `reservations.source`, `reservations.allReservations`, `reservations.filterAll`, `reservations.filterInquiries`, `reservations.filterFeeds`)

**CSS:**
- `.reservation-filters` — flex row of pill-button filters with active state
- `.reservation-filter` / `.reservation-filter.active` — styled pill buttons
- `.reservation-badges` — flex container for source badge + status select
- `.reservation-source-badge` / `.source-inquiry` — colored badge pills

**Files Changed:**
- `src/public/app.js` — i18n keys (EN/BG), rewritten `loadReservationsPage()` to fetch and merge both data sources with filter tabs
- `src/public/style.css` — filter tabs, source badges, badge container styles
- `docs/` — rebuilt static export

**Validation:**
- `npm run build` succeeds (13 events synced)
- Combined list shows 15 items (13 feed events + 2 inquiries)
- Filter tabs correctly toggle between All/Inquiries/Feeds
- Source badges display correct colors per feed

## Redesign: Reservations Page — Monthly Timeline Layout

**Problem:** The Reservations page displayed all items in a flat list, making it hard to scan and understand the temporal flow of bookings across months.

**Fix:** Redesigned the page with a modern month-grouped timeline layout:

**Features:**
- Events grouped by month with collapsible sections
- Month order: current month first (with gold "NOW" badge), then future months ascending, then past months descending
- Past months start collapsed (click to expand)
- Vertical timeline rail connecting events within each month (gold rail for current month)
- Colored timeline dots per source (gold for inquiries, source color for feeds)
- Day number circles for quick date scanning
- Month headers show summary: total reservations, inquiry count pill, feed count pill
- Cards have hover animation (translateX + shadow)
- Filter pills (All/Inquiries/Feeds) hide/show items AND auto-hide empty month sections

**i18n:** 3 new keys in EN and BG (`reservations.now`, `reservations.reservation`, `reservations.reservations`)

**CSS:**
- `.reservations-timeline` — flex column container
- `.tl-month` / `.tl-month-current` / `.tl-month-past` — month section states
- `.tl-month-header` — clickable collapse/expand header with hover effect
- `.tl-month-body` — timeline rail with left border
- `.tl-item` / `.tl-dot` / `.tl-dot-inquiry` — timeline items and colored dots
- `.tl-month-now` / `.tl-meta-pill` — badge pills
- `.reservation-day` — circular day number badge
- Card hover: `transform: translateX(4px)` + box-shadow
- Mobile responsive: adjusted padding, dot positions, header stacking

**Files Changed:**
- `src/public/app.js` — i18n keys, rewritten `loadReservationsPage()` with month grouping, collapse/expand, filter hiding
- `src/public/style.css` — complete timeline CSS rewrite + mobile responsive rules
- `docs/` — rebuilt static export

**Validation:**
- `npm run build` succeeds
- Timeline displays correctly with month sections
- Current month shows "NOW" badge and gold timeline rail
- Past months are collapsed by default, expandable on click
- Filter tabs hide items and empty month sections
- Mobile responsive with adjusted layout

**Note:** Disk corruption on E: drive affected multiple files (node_modules, server.js, pushService.js, sw.js, login.html, index.html, certificates, journal, wasm). All restored from GitHub remote + regenerated certs.

## Feature: URL-Based Routing with Language Prefixes

**What:** Added client-side routing so each page has a unique URL with language prefix (e.g., `/en/dashboard`, `/bg/settings`). Root `/` redirects to `/{savedLang}/dashboard`. Browser back/forward navigation works. Direct URL access works via SPA fallback on both Express and Cloudflare Workers.

**Client-Side Router (app.js):**
- `parseRoute(pathname)` — extracts `{ lang, page }` from URL path
- `VALID_LANGS` (`en`, `bg`), `VALID_PAGES` (10 pages) — route validation constants
- `navigateTo()` — now accepts `{ pushState, langOverride }` options, calls `history.pushState` to update URL
- `setLang()` — now accepts `{ updateUrl }` option, updates URL when language changes
- `popstate` listener — handles browser back/forward
- `updateNavHrefs()` — updates nav `href` attributes to `/{lang}/{page}` for accessibility
- `init()` — parses URL on load, sets language + page from URL, uses `history.replaceState`
- Static mode (`IS_STATIC`) skips URL routing entirely

**SPA Fallback:**
- Express (`src/server.js`): `GET /:lang(en|bg)/:page?` serves `index.html`
- Cloudflare Worker (`worker/src/index.js`): regex match on `/(en|bg)/*` rewrites to `/index.html`

**Asset Resolution:**
- Added `<base href="/">` to `src/public/index.html` so relative paths resolve correctly from `/en/dashboard`
- Static export (`scripts/export-static.js`) replaces with `<base href="./">` for GitHub Pages

**Files Changed:**
- `src/public/app.js` — router constants, parseRoute, modified navigateTo/setLang, popstate, init URL parsing
- `src/public/index.html` — `<base href="/">`, removed hardcoded `active` class from dashboard
- `src/server.js` — SPA fallback route
- `worker/src/index.js` — SPA fallback rewrite
- `scripts/export-static.js` — base tag replacement for static mode
- `docs/` — rebuilt static export

**Validation:**
- `npm run build` succeeds (13 events synced)
- `/en/dashboard` loads dashboard in English
- `/bg/dashboard` loads dashboard in Bulgarian (calendar locale, UI text, BG button active)
- `/en/settings` direct access works (SPA fallback serves index.html, client routes correctly)
- Language switching updates URL (e.g., `/en/settings` → `/bg/settings`)
- Page navigation updates URL (e.g., `/en/dashboard` → `/en/settings`)
- Browser back/forward navigates correctly
- Static export uses `<base href="./">` and skips URL routing

## Feature: Cron Jobs Admin Page

**What:** Added an admin-only "Cron Jobs" page to view scheduled tasks, their status, run history, and trigger manual runs. Also added persistent cron run history tracking to the database.

**Database:**
- New `cron_runs` table: id, job_id, status, details, run_at
- Added to both `worker/src/db.js` (D1 schema) and `src/db.js` (local SQLite)
- DB functions: `addCronRun(jobId, status, details)`, `getCronHistory()`

**API Endpoints:**
- `GET /api/admin/cron-jobs` (admin) — returns configured jobs with last run time, feed status, and run history
- `POST /api/admin/cron-jobs/:id/run` (admin) — triggers a manual cron run, logs to activity_logs

**Cron Run Tracking:**
- Express: hourly `node-cron` schedule now records success/error to `cron_runs` table
- Worker: `scheduled()` handler now records run results to D1 `cron_runs` table
- Both manual and automatic runs are persisted

**Frontend:**
- Nav item: `data-page="cron-jobs"` with clock icon, under admin-only section
- Page: `#page-cron-jobs` with job cards showing name, description, schedule, last/next run, feed status pills
- "Run Now" button triggers manual sync with inline status feedback
- History table shows recent runs with status badges and timestamps
- Auto-refreshes after manual run completes

**i18n:** 13 new keys in both EN and BG (`cronJobs.*`, `nav.cronJobs`)

**CSS:**
- `.cron-job-card`, `.cron-job-header`, `.cron-job-meta` — card layout with grid metadata
- `.cron-feed-pill`, `.cron-feed-ok`, `.cron-feed-error` — colored status pills
- `.cron-history-table` — styled table with status badges
- Mobile responsive: header stacks, meta grid collapses to single column

**Files Changed:**
- `src/public/index.html` — nav item + page HTML
- `src/public/app.js` — i18n keys (EN/BG), `loadCronJobsPage()`, router dispatch, VALID_PAGES
- `src/public/style.css` — cron job styles + mobile responsive
- `src/server.js` — cron run tracking, `GET/POST /api/admin/cron-jobs` routes
- `worker/src/index.js` — `GET/POST /api/admin/cron-jobs` routes, scheduled handler tracking
- `worker/src/db.js` — `cron_runs` table, `addCronRun()`, `getCronHistory()`
- `src/db.js` — `cron_runs` table, `addCronRun()`, `getCronHistory()`
- `docs/` — rebuilt static export

**Validation:**
- `npm run build` succeeds (13 events synced)
- Page renders at `/en/cron-jobs` with correct title and headings
- API returns 403 for non-admin users
- Server starts without errors

## Change: Reservations Page Public Access

**What:** Made the Reservations page visible to all users (not just admins). Only admins can edit inquiry statuses.

**Changes:**
- Nav item: removed `nav-admin-only` class — now visible to all visitors
- `loadReservationsPage()`: fetches `/api/events` (public) for all users; only fetches `/api/admin/inquiries` if user is admin
- Inquiry status dropdown: only rendered for admin users; non-admins see a read-only status badge instead

**Files Changed:**
- `src/public/index.html` — removed `nav-admin-only` from reservations nav item
- `src/public/app.js` — conditional data fetching and admin-only edit controls
- `CLAUDE.md` — updated workflow: commit and push automatically after changes are verified
- `docs/` — rebuilt static export

**Validation:**
- `npm run build` succeeds
- Non-admin users see Reservations in sidebar and can view feed events
- Inquiry data and status controls only visible for admins

## Enhancement: Push Notification on New Inquiry + Version Update Check

**Push Notification on Inquiry Submission:**
- `src/server.js` `POST /api/inquiries` now sends push notification to all subscribers via `pushService.sendToAll()`
- Notification title: "New Booking Inquiry", body includes check-in/out dates and guest count
- Notification click links to `/en/reservations`
- Generated VAPID keys and configured in `.env`
- Note: Worker runtime doesn't send push (no `web-push` library) — Express only for now

**Version Update Detection:**
- New endpoint `GET /api/version` returns MD5 hash of `app.js` content (both Express and Worker)
- Client stores version on init, polls `/api/version` every 5 minutes
- When mismatch detected, shows a fixed bottom banner: "A new version is available." with Refresh/Dismiss buttons
- Banner styled with accent color, high z-index, responsive
- Skipped in static mode (`IS_STATIC`)

**i18n:** 4 new keys in both EN and BG (`update.available`, `update.refresh`)

**Files Changed:**
- `src/server.js` — push notification on inquiry, `GET /api/version` endpoint, app version hash
- `worker/src/index.js` — `GET /api/version` endpoint using Web Crypto API
- `src/public/app.js` — version polling in `init()`, update banner creation, i18n keys
- `src/public/style.css` — `#update-banner` styles
- `docs/` — rebuilt static export

**Validation:**
- `npm run build` succeeds (13 events synced)
- `POST /api/inquiries` returns success, push notification code executes (no subscribers yet, so no actual notification sent)
- `GET /api/version` returns `{"version":"18f7b1b1"}`
- Client stores version and poll interval is active
- No console errors
- App renders correctly on all pages

## Fix: Cloudflare Worker 500 Errors on /api/version and SPA Routes

**Problem:** The live Cloudflare Workers deployment returned 500 errors on `GET /api/version` and SPA fallback routes like `/en/reservations`.

**Root Cause:** Two issues in `worker/src/index.js`:
1. `/api/version` used `new Request(new URL(...))` to fetch from `env.ASSETS` — the Request constructor with a URL object may not work correctly with the Workers ASSETS binding.
2. SPA fallback passed the original `request` as the second argument to `new Request()`, copying method/headers/body from the original request which could cause incompatibilities with static asset fetching.

**Fix:** Changed both to pass `url.toString()` (a plain string) to `env.ASSETS.fetch()` instead of constructing a new Request object:
- `/api/version`: `env.ASSETS.fetch(appUrl.toString())`
- SPA fallback: `env.ASSETS.fetch(indexUrl.toString())`

**Files Changed:**
- `worker/src/index.js` — simplified ASSETS.fetch calls

**Fix (updated):** The actual root cause was that `env.ASSETS` was `undefined` because `wrangler.jsonc` had `assets.directory` but no `assets.binding`. Added `"binding": "ASSETS"` to make the ASSETS fetcher available in worker code.

**Files Changed:**
- `wrangler.jsonc` — added `"binding": "ASSETS"` to assets config
- `worker/src/index.js` — cleaned up debug error handling

**Validation:**
- `npm run build` succeeds
- Deployed via git push (auto-deploys from main branch)
- `GET /api/version` → `{"version":"58541f73"}` ✓
- `GET /en/reservations` → 200 (SPA HTML) ✓
- `GET /bg/settings` → 200 (SPA HTML) ✓
- `GET /en` → 200 (dashboard) ✓
- `GET /api/events` → 200 (existing endpoint unaffected) ✓

## Added Exceptions Page, Changelog Page, and Auto-Versioning

### 1. Exceptions Page (admin-only)
**What:** Logs all client-side JavaScript errors (window.onerror + unhandledrejection) to the database and shows them in an admin-only Exceptions page.

**Implementation:**
- New `client_errors` DB table (id, message, source, lineno, colno, stack, url, user_agent, user_id, created_at)
- `POST /api/errors` — public endpoint to log errors (auto-attaches user_id if logged in, UA from header)
- `GET /api/admin/errors` — admin-only, returns last 200 errors
- `DELETE /api/admin/errors` — admin-only, clears all errors
- Global `window.addEventListener('error', ...)` and `unhandledrejection` handlers in app.js
- Exceptions page with table showing timestamp, message (with expandable stack trace), source, line:col, page URL
- Clear All (red) and Refresh buttons

### 2. Changelog Page (public)
**What:** A public-facing page showing version history with short summaries.

**Implementation:**
- `CHANGELOG.json` at project root + `src/public/CHANGELOG.json` (must stay in sync)
- `GET /api/changelog` — reads from CHANGELOG.json (Worker fetches via ASSETS binding, Express reads from fs)
- Changelog page renders version badges, dates, and bullet-pointed changes
- Available in static export (copied to docs/)
- Initial history populated with versions 1.0.0 through 1.7.0

### 3. Auto-Versioning via CLAUDE.md
**What:** Updated CLAUDE.md step 5 → step 6, inserted new step 5 requiring changelog bump before every commit.

**Rules:**
- Patch (x.x.1) for bug fixes, Minor (x.1.0) for features, Major (x.0.0) for breaking changes
- New entry at top of CHANGELOG.json array with version, date, 1-3 bullet points
- Both CHANGELOG.json files and package.json version must stay in sync

**Files Changed:**
- `worker/src/db.js` — `client_errors` table, `addClientError()`, `getClientErrors()`, `clearClientErrors()`
- `worker/src/index.js` — `POST /api/errors`, `GET/DELETE /api/admin/errors`, `GET /api/changelog`
- `src/db.js` — same table + functions for Express
- `src/server.js` — same API endpoints for Express
- `src/public/app.js` — i18n keys (EN+BG), VALID_PAGES, navigateTo dispatches, page loaders, global error handlers
- `src/public/index.html` — nav items (exceptions admin-only, changelog public), page divs
- `src/public/style.css` — changelog and exceptions styles
- `CHANGELOG.json` + `src/public/CHANGELOG.json` — version history
- `CLAUDE.md` — added step 5 (changelog bump before commit)
- `package.json` — version bumped to 1.7.0
- `scripts/export-static.js` — copies CHANGELOG.json to docs/

**Validation:**
- `npm run build` succeeds (13 events, CHANGELOG.json copied)
- Changelog page renders all 8 versions in EN and BG
- `GET /api/changelog` returns 8 entries, latest 1.7.0
- `POST /api/errors` logs successfully
- Exceptions page shows "No client errors recorded." when empty
- Admin-only nav items (Exceptions) hidden from non-admins
- No console errors
